# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config
.bundle

# Ignore all environment files
.env*

# Ignore all logfiles and tempfiles
/log/*
/tmp/*
/log/.keep
/tmp/.keep

# Ignore pidfiles, but keep the directory
/tmp/pids/*
/tmp/pids/
/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases)
storage/*
/storage/.keep
/tmp/storage/*
/tmp/storage/
/tmp/storage/.keep

# Ignore public assets (compiled by asset pipeline)
# This may include precompiled assets in production, but also in development.
# We don't want to version control compiled assets, as they can be rebuilt.
public/assets

# Ignore master key for decrypting credentials and more
/config/master.key

# Ignore build files from asset pipeline (e.g. if using webpacker)
app/assets/builds/*
/app/assets/builds/.keep

# Ignore node_modules (installed npm packages)
node_modules/

# Ignore by default yarn.lock and package-lock.json files.
# These can be tracked by default if you have the intention of tracking npm dependencies
# (e.g. in certain cases where npm is part of the app, like in fullstack projects).
yarn-error.log
package-lock.json

# Ignore MacOS system files
.DS_Store

# Ignore Windows system files
Thumbs.db
ehthumbs.db

# Ignore temporary files created by IDEs/editors
.idea/
.vscode/
*.swp
*.swo
*.sublime-workspace
*.sublime-project

# Ignore logs generated by test frameworks
coverage/

# Ignore temporary files
*.bak
*.tmp

# Ignore any other unnecessary files or folders
public/uploads/

# Ignore logs (ensure .keep is preserved to keep log directories)
log/*
/log/.keep  # Deixe o arquivo .keep para garantir que o diretório log seja preservado

# Ignore the server.pid file
/tmp/pids/*
/tmp/pids/.keep

# Ignore temporary files, like cache, pids, and others
/tmp/*
/tmp/.keep
/tmp/cache
/tmp/restart.txt

# Ignore test logs
/log/test.log
/log/development.log

# Ignore temporary files from editors/IDEs
*.swp
*.swo
*.sublime-workspace
*.sublime-project

# Ignore node_modules (dependências do Node.js)
node_modules/

# Ignore test logs if the test directory is present
test.log
/test/

.kamal/
vendor/
